use C: std.c;

val STDIN: i32 = 0;
val STDOUT: i32 = 1;
val STDERR: i32 = 2;

fun print(s: string) {
    C.printf(s);
}

fun write(s: string) {
    val size: i64 = len(s);

    ret C.write(STDOUT, s, size);
}
